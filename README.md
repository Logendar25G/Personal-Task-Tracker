# Personal-Task-Tracker
A simple Personal Task Tracker CLI application built using TypeScript. It allows users to add, update, delete, filter, and view tasks directly from the terminal. Tasks are stored in memory, and each task includes status, priority, due date, and completion tracking.
